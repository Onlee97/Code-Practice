#include <string>
#include <iostream>

using namespace std;

class solution{
	static string longestSubstring(string abstring){
		if (abstring == NULL || abstring.size() == 0) return abstring;
		int startIdx = 0;
		int endIdx = 0;
		int aCount = 0;
		int bCount = 0;
		while(endIdx < abstring.size()){
			
		}

	}
};